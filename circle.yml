machine:
  node:
    version: 5.5.0

dependencies:
  override:
   - npm install frisby
   - npm install jasmine-node
   - npm install ntwitter

test:
  override:
   - echo Hello World
   - jasmine-node test_spec.js
